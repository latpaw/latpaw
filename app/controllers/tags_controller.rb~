class TagsController < ApplicationController


 def destroy
    @post = Post.find(params[:post_id])
    @comment = @post.tags.find(params[:id])
    @comment.destroy
    redirect_to post_path(@post)
  end
  
end
